// src/notifications/notify.android.js
// No-op for now so the app compiles on Android.
// We can add Android notifications later if you want.
export function initNotifications() {}
export function cancelAllScheduledReminders() {}
export async function rescheduleAll() {}
